import {config} from "rxjs";

export const BACKEND_CONFIG_ROOT = "http://localhost:8080/config" //for the backend address
export const BACKEND_CONFIG_URL = '${BACKEND_CONFIG_ROOT}`/config' //for the backend address
